#include <iostream>
#include <vector>

using namespace std;


int findMin(const vector<int> &v);

int main(){
    
    
    
    return 0;
}

int findMin(const vector<int> &v8)
{
    if (v.size != 1)
    {
        if (v.at(v.size()-1)) > v.at(v.size()-2))
        {
            
        }
    }
    return v.at(0);
}
